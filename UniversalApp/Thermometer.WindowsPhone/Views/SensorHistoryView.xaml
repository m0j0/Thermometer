<UserControl
    x:Class="Thermometer.Views.SensorHistoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Thermometer.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Thermometer.ViewModels"
    mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:SensorHistoryVm}"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid>
<Grid.RowDefinitions>
    <RowDefinition Height="Auto" />
    <RowDefinition Height="Auto" />
    <RowDefinition Height="*" />
</Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0"
            Text="Период показаний:" />
        <ComboBox Grid.Row="0" Grid.Column="1"
                  ItemsSource="{Binding PeriodTypes}"
                  SelectedValue="{Binding SelectedPeriodType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  SelectedValuePath="Item1" DisplayMemberPath="Item2"/>
    </Grid>
</UserControl>
